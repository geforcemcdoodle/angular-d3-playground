<svg:circle
  class="circle gesture__zone"
  [attr.cx]="node.sim.x"
  [attr.cy]="node.sim.y"
  [attr.r]="node.r"  
  (press)="onPress($event)"
  (pressUp)="onPressUp($event)"
  (tap)="onClick()"
  stroke="black"
/>