<svg:circle
  class="circle gesture__zone"
  [attr.cx]="node.x"
  [attr.cy]="node.y"
  [attr.r]="radius"  
  (press)="onPress($event)"
  (pressUp)="onPressUp($event)"
  fill="#0077B6"
  (tap)="onClick()"
/>