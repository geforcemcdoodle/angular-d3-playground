<svg:circle
  class="circle gesture__zone"
  [attr.cx]="node.x"
  [attr.cy]="node.y"
  [attr.r]="radius"  
  (press)="onPress($event)"
  (pressUp)="onPressUp($event)"
  (tap)="onClick()"
  stroke="black"
/>